Para executar o projeto, baixe o arquivo .zip,
descompacte,
vá ate a pasta API no terminal,
execute ".\mvnw clean install" para a build,
depois da build ser concluída execute ".\mvnw spring-boot:run" para rodar a API.
